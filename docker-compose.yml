services:
  timetable:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tiet-timetable
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - PORT=${PORT:-8080}
    restart: unless-stopped